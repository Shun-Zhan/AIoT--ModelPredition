# Cross-platform installation entry point.
# Dependency ranges live in pyproject.toml so pip can select the correct wheel
# for the user's operating system and Python version (Windows/macOS/Linux).
-e .[test]
