# Real cloud calls are opt-in. The startup scripts inherit these variables
# from the terminal; they do not read this file automatically.
AIOT_LLM_ENABLED=0
VEI_API_KEY=
VEI_BASE_URL=https://ai-gateway.vei.volces.com/v1
VEI_MODEL=填写火山引擎控制台提供的模型名称或推理接入点ID
VEI_TIMEOUT_SECONDS=45

# Periodic cloud analysis is non-real-time and defaults to once per 15 min.
AIOT_LLM_INTERVAL_MINUTES=15

# Keep disabled for normal use. When enabled, accepted suggestions skip the
# web confirmation click but still pass every local safety rule.
AIOT_DEMO_AUTO_EXECUTE=0
